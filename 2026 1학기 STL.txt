//------------------------------------------------------------------
// 2026 1학기 STL				3월 3일					(1주 1일)
//------------------------------------------------------------------
// VS 버전		- 17.14.27
// 컴파일 환경	- Release / x64
// 프로젝트 설정 - 일반 - C++ 언어 표준 - /std::c++latest
//				- C/C++ - SDL 설정 - 아니요
//------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>

void save( const std::string& );

int main()
{
	std::cout << "2026년 3월 3일" << '\n';

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void save( const std::string& fname )
{
	std::cout << '\"' << fname << "\" 저장합니다" << '\n';

	std::ifstream in { fname };		// RAII - Resource Acquisition Is Initialization
	if ( not in )
	{
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260303);
	}

	std::ofstream out { "2026 1학기 STL.txt", std::ios::app };

	// in의 내용을 읽어서 out에 덧붙이자.
	std::copy(std::istreambuf_iterator<char>{ in }, {},
		std::ostream_iterator<char>{ out });
}

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save( const std::string& );

int main()
{

	std::cout << "2026년 3월 4일" << '\n';

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void save( const std::string& fname )
{
	std::cout << '\"' << fname << "\" 저장합니다" << '\n';

	std::ifstream in { fname };		// RAII - Resource Acquisition Is Initialization
	if ( not in )
	{
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out { "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now();
	std::cout << now << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본)
	std::copy(std::istreambuf_iterator<char>{ in }, {},
		std::ostream_iterator<char>{ out });
}

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save( const std::string& );

int main()
{

	std::cout << "2026년 3월 4일" << '\n';

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void save( const std::string& fname )
{
	std::cout << '\"' << fname << "\" 저장합니다" << '\n';

	std::ifstream in { fname };		// RAII - Resource Acquisition Is Initialization
	if ( not in )
	{
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out { "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	auto now = std::chrono::system_clock::now();
	using namespace std::chrono_literals;
	std::cout << now + 9h << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본)
	std::copy(std::istreambuf_iterator<char>{ in }, {},
		std::ostream_iterator<char>{ out });
}

====================================================================
저장 시간 - 2026-03-04 11:56:32.2519782
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
#include <array>
#include <chrono>

void save( const std::string& );

int main()
{

	std::cout << "2026년 3월 4일" << '\n';

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void save( const std::string& fname )
{
	std::cout << '\"' << fname << "\" 저장합니다" << '\n';

	std::ifstream in { fname };		// RAII - Resource Acquisition Is Initialization
	if ( not in )
	{
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out { "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 저장하자
	auto now = std::chrono::system_clock::now();
	using namespace std::chrono_literals;
	
	out << "===================================================================" << '\n';
	out << "저장 시간 - " << now + 9h << '\n';
	out << "===================================================================" << '\n';
	out << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본)
	std::copy(std::istreambuf_iterator<char>{ in }, {},
		std::ostream_iterator<char>{ out });
}

====================================================================
저장 시간 - 2026-03-04 12:25:23.3320253
====================================================================

//------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026. 3. 4												jihoon
//------------------------------------------------------------------

#pragma once

void save(const std::string&);

====================================================================
저장 시간 - 2026-03-04 12:25:23.3404261
====================================================================

//------------------------------------------------------------------
// 한 학기 강의를 저장하는 함수
// 
// 2026. 3. 4												jihoon
//------------------------------------------------------------------

#include <chrono>
#include <iostream>
#include <fstream>

#include "save.h"

void save(const std::string& fname)
{
	std::cout << '\"' << fname << "\" 저장합니다" << '\n';

	std::ifstream in{ fname };		// RAII - Resource Acquisition Is Initialization
	if (not in)
	{
		std::cout << fname << " - 열 수 없습니다" << '\n';
		exit(20260304);
	}

	std::ofstream out{ "2026 1학기 STL.txt", std::ios::app };

	// 저장한 시간을 out에 기록하자
	// std::chrono::system_clock::time_point now = std::chrono::system_clock::now();
	// 시간을 한글로 저장하자
	auto now = std::chrono::system_clock::now();
	using namespace std::chrono_literals;

	out << "====================================================================" << '\n';
	out << "저장 시간 - " << now + 9h << '\n';
	out << "====================================================================" << '\n';
	out << '\n';

	// in의 내용을 읽어서 out에 덧붙이자.
	// std::copy(원본, 복사본)
	std::copy(std::istreambuf_iterator<char>{ in }, {},
		std::ostream_iterator<char>{ out });

	out << '\n';
}

====================================================================
저장 시간 - 2026-03-04 12:25:23.3473418
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// save한 시간을 기록한다
// save를 분리한다
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main()
{

	std::cout << "2026년 3월 4일 - save 완성" << '\n';

	save( "save.h" );
	save( "save.cpp" );
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-04 12:34:15.9480610
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

void change(int&, int&);

int main()
{
	int a{ 1 }, b{ 2 };

	// [문제] change를 호출하여 다음과 같이 출력되게 하라.
	change(a, b);

	std::cout << a << ", " << b << std::endl;		//  2, 1

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

====================================================================
저장 시간 - 2026-03-04 12:57:28.1708407
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

	operator int() {
		return value;
	}

private:
	int value{};
};

void change(int&, int&);
void change(Dog&, Dog&);

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

====================================================================
저장 시간 - 2026-03-04 13:01:07.4133650
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

void change(int&, int&);
void change(Dog&, Dog&);

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

void change(int& a, int& b)
{
	int temp{ a };
	a = b;
	b = temp;
}

void change(Dog& a, Dog& b)
{
	Dog temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

====================================================================
저장 시간 - 2026-03-04 13:17:57.0742781
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

template <class T>
void change(T&, T&);

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change<int>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change<Dog>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

template <class T>
void change(T& a, T& b)
{
	T temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

====================================================================
저장 시간 - 2026-03-04 13:18:42.1261534
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 4일					(1주 2일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

template <class T>
void change(T& a, T& b)
{
	T temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change<int>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change<Dog>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 14:19:33.0975261
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

// compiler phase - cppreference

template <class T>
void change(T& a, T& b)
{
	T temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

template <>
void change<Dog>(Dog& a, Dog& b)
{
	Dog temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

int main()
{


	{
		int a{ 1 }, b{ 2 };
		change<int>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change<Dog>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 14:26:50.0742669
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

// compiler phase - cppreference

template <class T>
void change(T& a, T& b)
{
	T temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

void change(Dog& a, Dog& b)
{
	std::cout << "나는 global 함수야" << '\n';
}

template <>		// specialization
void change<Dog>(Dog& a, Dog& b)
{
	std::cout << "누가 개소리를 내었는가?" << '\n';
}

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change<int>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change<Dog>(a, b);								// 함수 호출 우선순위
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 14:27:10.3235817
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog;				// forward declaration

// compiler phase - cppreference

template <class T>
void change(T& a, T& b)
{
	T temp{ a };		// copy constructor
	a = b;				// a.operator= (b);
	b = temp;
}

class Dog {
public:
	Dog() = default;
	Dog(int input) : value{ input } {}

private:
	int value{};

	friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
		return os << dog.value;
	}
};

void change(Dog& a, Dog& b)
{
	std::cout << "나는 global 함수야" << '\n';
}

template <>		// specialization
void change<Dog>(Dog& a, Dog& b)
{
	std::cout << "누가 개소리를 내었는가?" << '\n';
}

int main()
{
	{
		int a{ 1 }, b{ 2 };
		change<int>(a, b);
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	{
		Dog a{ 1 }, b{ 2 };
		change(a, b);									// 함수 호출 우선순위
		std::cout << a << ", " << b << std::endl;		//  2, 1
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 14:48:51.9743736
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>
#include "save.h"

template <class T>
T add(T a, T b)
{
	return a + b;
}

int main()
{
	// [문제] template 함수 add로 실행되게 하자

	std::cout << add<int>(2026, 310) << std::endl;
	using namespace std::string_literals;
	std::cout << add<std::string>("2026년"s, " 3월 10일"s) << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 14:52:25.2474615
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// template
//------------------------------------------------------------------

#include <iostream>
#include "save.h"

template <class T>
T add(const T& a, const T& b)
{
	return a + b;	// RVO
}

int main()
{
	// [문제] template 함수 add로 실행되게 하자

	std::cout << add<int>(2026, 310) << std::endl;
	using namespace std::string_literals;
	std::cout << add<std::string>("2026년"s, " 3월 10일"s) << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 15:00:41.3377574
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// C++언어에서 template의 역할은? -> Generic Programming
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main()
{
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-10 15:19:11.4020597
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 10일					(2주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i)
	{
		num[i] = uid(dre);
	}

	for (int n : num)
	{
		std::print("{:^80}", n);
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 11:39:02.7103366
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}

	// [문제] 가장 큰 값을 찾아 화면에 출력하자
	int max = std::numeric_limits<int>::min();

	for (int n : num) {
		if (n > max) {
			max = n;
		}
	}

	std::print("\n가장 큰 수 : {}\n", max);

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 12:03:37.6602473
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <algorithm>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값을 찾아 화면에 출력하자
	auto maxValuePosition = std::max_element(std::begin(num), std::end(num));
	std::cout << "가장 큰 값은 - " << *maxValuePosition << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 12:11:26.0233890
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <algorithm>

#include "save.h"

std::default_random_engine dre{ std::random_device{}() };
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}
	std::cout << std::endl;

	// [문제] 가장 작은 값을 찾아 화면에 출력하자
	auto maxValPos = std::min_element(std::begin(num), std::end(num));
	std::cout << "가장 큰 값은 - " << *maxValPos << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 12:24:11.7295610
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <algorithm>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	auto minMaxValPos = std::minmax_element(std::begin(num), std::end(num));
	std::cout << "가장 작은 값은 - " << *minMaxValPos.first << std::endl;
	std::cout << "가장 큰 값은 - " << *minMaxValPos.second << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 12:25:15.8465390
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <algorithm>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 9999 };

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	int num[1000];

	for (int i = 0; i < 1000; ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}	
	std::cout << std::endl;

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	// structured binding - syntactic sugar
	auto [minValPos, maxValPos] = std::minmax_element(std::begin(num), std::end(num));
	std::cout << "가장 작은 값은 - " << *minValPos << std::endl;
	std::cout << "가장 큰 값은 - " << *maxValPos << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 12:54:45.9751527
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <algorithm> 
#include <array>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 9999 };

template <class T, int N>
class myArray {
public:
	T& operator[](int idx) {
		return arr[idx];
	}

	T* begin() {
		return &arr[0];
	}

	T* end() {
		return &arr[N];
	}

	size_t size() {
		return N;
	}

private:
	T arr[N];
};

int main()
{
	// [문제] 임의의 값을 갖는 int 1000개를 생성하라. 모든 값을 화면에 출력하라.

	// int num[1000];		// 이건 자료구조는 사용하지 말자
	myArray<int, 1000> num{};

	for (int i = 0; i < num.size(); ++i) {
		num[i] = uid(dre);
	}

	std::cout << "랜덤 int 1000개" << std::endl;

	for (int n : num) {
		std::print("{:8}", n);
	}
	std::cout << std::endl;

	// [문제] 가장 큰 값과 가장 작은 값을 찾아 화면에 출력하자
	// structured binding - syntactic sugar
	auto [minValPos, maxValPos] = std::minmax_element(num.begin(), num.end());
	std::cout << "가장 작은 값은 - " << *minValPos << std::endl;
	std::cout << "가장 큰 값은 - " << *maxValPos << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 13:10:18.0351901
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <fstream>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 999'9999 };

int main()
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하시오
	std::ofstream out { "int값1000만개.txt" };

	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << ' ';
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-11 13:10:28.4769459
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <fstream>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 999'9999 };

int main()
{
	// [문제] 랜덤 int값 1000만개를 "int값1000만개.txt"에 저장하시오
	std::ofstream out { "int값1000만개.txt" };

	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << ' ';
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 13:33:04.2454765
====================================================================

//--------------------------------- ---------------------------------
// 2026 1학기 STL				3월 11일					(2주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <fstream>
#include <array>

#include "save.h"

std::default_random_engine dre{ };
std::uniform_int_distribution<int> uid{ 0, 999'9999 };

int main()
{
	// [문제] "int값1000만개.txt"에 저장한 int값을 읽어 화면에 저장하라.
	/*std::ofstream out { "int값1000만개.txt" };

	for (int i = 0; i < 1000'0000; ++i) {
		out << uid(dre) << ' ';
	}*/

	std::ifstream in{ "int값1000만개.txt" };

	if (not in) {
		std::cout << "파일 읽기 실패" << std::endl;
		return 2022184025;
	}

	/*int num = 0;
	while (in >> num) {
		std::cout << num << std::endl;
	}*/

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 13:55:29.3068708
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------
// 오답 예시

#include <iostream>
#include <fstream>

#include "save.h"

int main()
{
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값 중에서 가장 작은 값을 찾아 출력하라.
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실하다
	
	std::ifstream in{ "int값1000만개.txt" };

	if (!in) {
		std::cout << "파일 읽기 실패!" << std::endl;
		return 20260317;
	}
	
	int min = std::numeric_limits<int>::max();
	int num_buffer = std::numeric_limits<int>::max();

	while (in >> num_buffer) {
		if (num_buffer < min) {
			min = num_buffer;
		}
	}

	std::cout << "가장 작은 값은 : " << min << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 14:04:29.4155480
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------
// 오답 예시

#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>

#include "save.h"

std::array<int, 1000'0000> a;

int main()
{
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값 중에서 가장 작은 값을 찾아 출력하라.
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실하다

	std::ifstream in{ "int값1000만개.txt" };

	if (!in) {
		std::cout << "파일 읽기 실패!" << std::endl;
		return 20260317;
	}

	for (int& num : a) {
		in >> num;
	}
	
	std::cout << "최솟값 - " << *std::min_element(a.begin(), a.end()) << std::endl;


	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 14:12:01.0320756
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기
//------------------------------------------------------------------
// ***정답 예시***

#include <iostream>
#include <fstream>
#include <algorithm>

#include "save.h"

int main()
{
	// [문제] text모드로 저장된 "int값1000만개.txt"에 저장되어 있는 int값 중에서 가장 작은 값을 찾아 출력하라.
	// 가장 작은 값을 찾아 출력하라. int는 천만개가 확실하다

	std::ifstream in{ "int값1000만개.txt" };

	if (!in) {
		std::cout << "파일 읽기 실패!" << std::endl;
		return 20260317;
	}
	
	std::cout << "최솟값 - " << *std::min_element(std::istream_iterator<int>{in}, {}) << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

// 작년 문제
// [문제] int로 표현 가능한 모든 수를 파일에 text로 저장하였다. 수와 수는 한 개의 공백문자로
// 분리하였다. int 한 개를 저장하기 위한 평균 바이트를 계산하라.

====================================================================
저장 시간 - 2026-03-17 14:58:08.9383567
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <fstream>

#include "save.h"

int main()
{
	// [문제] int 1억개를 binary 모드로 파일에 저장.
	// binary모드 - 추가 가공 없이 데이터를 바이트 단위에서 바로 옮김
	// 텍스트와 바이너리 모드의 차이점은?
	// 개행 문자의 여부

	std::ofstream out{ "int1억개", std::ios::binary };

	for (int i = 0; i < 1'0000'0000; ++i) {
		// write(어디를, 몇개나). DMA(Direct Memory Access)로, CPU 관여 없이 메모리와 디스크 간에 즉각적으로 소통함
		out.write(reinterpret_cast<char*>(&i), sizeof(int));
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 15:04:50.5298782
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <array>
#include <numeric>
#include <print>

#include "save.h"

int main()
{
	// [문제] int 1000개를 다음과 같이 binary 모드로 파일에 기록하였다.
	std::array<int, 1000> a;

	std::iota(a.begin(), a.end(), 1);

	for (int num : a) {
		std::print("{:8}", num);
	}
	
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 15:08:50.4380409
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <array>
#include <numeric>
#include <print>

#include "save.h"

int main()
{
	// [문제] int 1000개를 다음과 같이 binary 모드로 파일 "int 천개.bin"에 기록하였다.
	std::array<int, 1000> a;

	std::iota(a.begin(), a.end(), 1);

	std::ofstream out{ "int 천개.bin", std::ios::binary };
	out.write(reinterpret_cast<char*>(&a[0]), a.size() * sizeof(int));
	
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-17 15:14:39.2570115
====================================================================

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 17일					(3주 1일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <array>
#include <print>

#include "save.h"

int main()
{
	// [문제] 파일 "int 천개.bin"에는 1000개의 int값이 저장되어있다.
	// 파일은 binary mode
	// 메모리에 int 1000개 값을 읽어 오세요.

	std::ifstream in{ "int 천개.bin", std::ios::binary };
	
	if (not in) {
		std::cout << "잘못된 파일입니다" << std::endl;
	}

	std::array<int, 1000> a;

	in.read(reinterpret_cast<char*>(a.data()), a.size() * sizeof(int));
	
	for (int num : a) {
		std::print("{:8}", num);
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 18일					(3주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <string>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
    Dog() {
        num = uid(dre);
        size_t len = uidNameLen(dre);
	for (int  i = 0; i < len; ++i) {
	    name += uidChar(dre);
    	}
    }
private:
    std::string name;		// 10 이상 30 이하 임의의 소문자
    int num{uid(dre)};

    friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
	std::print(os, "[{:4}] - {}", dog.num, dog.name);
	return os;
    }
};

int main()
{
	for (int i = 0; i < 10; ++i)
		std::cout << Dog() << std::endl;
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 18일					(3주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <string>
#include <array>
#include <fstream>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
    Dog() {
        num = uid(dre);
        size_t len = uidNameLen(dre);
	for (int  i = 0; i < len; ++i) {
	    name += uidChar(dre);
    	}
    }
private:
    std::string name;		// 10 이상 30 이하 임의의 소문자
    int num{uid(dre)};

    friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
	    std::print(os, "[{:4}] - {}", dog.num, dog.name);
    	return os;
    }
};

int main()
{
    // [문제] "dogs를 파일 "Dog백마리"에 저장하라.
    // 파일 크기는 sizeof(Dog) * dogs.size() 이어야 한다.
    std::array<Dog, 100> dogs;

    std::ofstream out{ "Dog백마리", std::ios::binary};
    out.write(reinterpret_cast<char*>(dogs.data()), dogs.size() * sizeof(Dog));
    
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 18일					(3주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include <filesystem>		// C++17

#include "save.h"

class Dog {
	
private:
    std::string name;		// 10 이상 30 이하 임의의 소문자
    int num;

    friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
	    std::print(os, "[{:4}] - {}", dog.num, dog.name);
    	return os;
    }
};

int main()
{
    // [문제] 바이너리 모드로 저장한 파일 "Dog백마리"에는 Dog 객체를 저장하였다.
	// Dog 객체는 파일의 write 함수를 사용하여 객체 크기 그대로 저장하였다.

	// 몇 객체인지 확인할 수 있는 방법?
	// 1 - 파일 사이즈 / sizeof(Dog)
	// 2 - 직접 읽어 몇 개인지 센다.
	
	std::ifstream in{ "Dog백마리", std::ios::binary };

	if ( not in ) {
		std::cout << "파일을 열 수 없습니다" << std::endl;
		return 2022184025;
	}

	std::cout << "Dog의 수 - " << std::filesystem::file_size("Dog백마리") / sizeof(Dog) << std::endl;
    
	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 18일					(3주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------
// 오류 코드 예시임.

#include <iostream>
#include <print>
#include <string>
#include <array>
#include <fstream>
#include <filesystem>		// C++17

#include "save.h"

class Dog {
	
private:
    std::string name;		// 10 이상 30 이하 임의의 소문자
    int num;

    friend std::ostream& operator<<(std::ostream& os, const Dog& dog) {
	    std::print(os, "[{:4}] - {}", dog.num, dog.name);
    	return os;
    }

	friend std::istream& operator>>(std::istream& is , const Dog& dog) {
		is.read(( char* )&dog , sizeof(Dog));
		return is;
	}
};

int main()
{
    // [문제] 바이너리 모드로 저장한 파일 "Dog백마리"에는 Dog 객체를 저장하였다.
	// Dog 객체는 파일의 write 함수를 사용하여 객체 크기 그대로 저장하였다.

	// 몇 객체인지 확인할 수 있는 방법?
	// 1 - 파일 사이즈 / sizeof(Dog)
	// 2 - 직접 읽어 몇 개인지 센다.
	
	std::ifstream in{ "Dog백마리", std::ios::binary };

	if ( not in ) {
		std::cout << "파일을 열 수 없습니다" << std::endl;
		return 2022184025;
	}

	Dog dog;
	size_t cnt{};

	while ( in >> dog ) {
		++cnt;
	}

	std::cout << "Dog의 수 - " << cnt << std::endl;

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}

//-------------------------------------------------------------------
// 2026 1학기 STL				3월 18일					(3주 2일)
//------------------------------------------------------------------
// 많은 수의 자료를 처리하기 - binary I/O
//------------------------------------------------------------------

#include <iostream>
#include <string>
#include <fstream>
#include <random>
#include <print>
#include <array>
#include <algorithm>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uid{ 0, 9999 };
std::uniform_int_distribution uidNameLen{ 10, 30 };
std::uniform_int_distribution<int> uidChar{ 'a', 'z' };

class Dog {
public:
	Dog( ) {
		num = uid(dre);
		size_t len = uidNameLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	std::string getName() const {
		return name;
	}
private:
	std::string name;		// 10 이상 30 이하 임의의 소문자
	int num{ uid(dre) };

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:4}] - {}" , dog.num , dog.name);
		return os;
	}
};


int main()
{
	std::array<Dog , 1000> dogs;
	
	std::sort(dogs.begin( ) , dogs.end( ) , [](const Dog& a , const Dog& b) {
		return a.getName( ) < b.getName( );
		});

	for ( const Dog& dog : dogs ) {
		std::cout << dog << std::endl;
	}

	save( "main.cpp" );		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-03-24 14:06:04.9462204
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 24일					(4주 1일)
//------------------------------------------------------------------
// 비주얼 스튜디오의 string 관리 방식 - SSO(Small String Optimization)
// 짧은 문자열은 heap 메모리 할당이 아니라 객체 내부 스택 버퍼에 바로 저장
// 
// 
// 동적 메모리 할당(Dynamic memory allocation)
// 
// Static - 컴파일 타임에 필요한 메모리가 결정됨
// Dynaimc - 런타임에 필요한 메모리가 결정됨
// 
// 메모리는 OS가 관리함
// 컴파일 타임엔 stack, data 구획 사용
// 런타임엔 free-store(heap) 구획 사용
// 
// 컴파일 타임에 사용되는 메모리는 이름을 붙여 사용할 수 있다.
// 런타임에 사용되는 메모리는 이름을 붙일 수 없다.
// 이름은 결정된 주소가 있어야 사용 가능함 (heap은 런타임에 결정되어 불가능)
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <numeric>

#include "save.h"


// [문제] 사용자가 원하는 개수 만큼 int를 저장할 메모리를 확보하라.
// 메모리를 1부터 시작하는 int 값으로 채워라.
// 합계를 계산하여 출력하라.
// 내 답안

int main( )
{
	std::cout << "원하는 만큼의 int 개수 입력: ";
	int arr_size;
	std::cin >> arr_size;

	// 문제없는 수만 입력할거라 희망

	std::vector<int> arr(arr_size);

	std::iota(arr.begin( ) , arr.end( ) , 1);
	
	long long sum = std::accumulate(arr.begin( ) , arr.end( ) , 0LL);

	std::cout << "합계 : " << sum << std::endl;


	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-24 14:37:41.0007888
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 24일					(4주 1일)
//------------------------------------------------------------------
// 비주얼 스튜디오의 string 관리 방식 - SSO(Small String Optimization)
// 짧은 문자열은 free store 메모리 할당이 아니라 객체 내부 스택 버퍼에 바로 저장
// 
// 
// 동적 메모리 할당(Dynamic memory allocation)
// 
// Static - 컴파일 타임에 필요한 메모리가 결정됨
// Dynaimc - 런타임에 필요한 메모리가 결정됨
// 
// 메모리는 OS가 관리함
// 컴파일 타임엔 stack, data 구획 사용
// 런타임엔 free-store 구획 사용
// 
// 컴파일 타임에 사용되는 메모리는 이름을 붙여 사용할 수 있다.
// 런타임에 사용되는 메모리는 이름을 붙일 수 없다.
// 이름은 결정된 주소가 있어야 사용 가능함 (free store는 런타임에 결정되어 불가능)
//------------------------------------------------------------------

#include <iostream>
#include <numeric>

#include "save.h"


// [문제] 사용자가 원하는 개수 만큼 int를 저장할 메모리를 확보하라.
// 메모리를 1부터 시작하는 int 값으로 채워라.
// 합계를 계산하여 출력하라.
// 오답

int main( )
{
	while ( true ) {
		std::cout << "원하는 만큼의 int 개수 입력: ";
		int num;
		std::cin >> num;

		// 문제없는 수만 입력할거라 희망

		int* p;	// 안 써야할 자료형

		try {
			// contiguous 메모리를 요청함
			p = new int[ num ];
		}
		catch ( std::exception& e ) {
			std::cout << "더 이상 줄 메모리가 없다 - " << e.what( ) << std::endl;
			exit(2022184025);
		}

		std::iota(p , p + num , 1);

		long long sum{};
		for ( int i = 0; i < num; ++i ) {
			sum += p[ i ];
		}

		std::cout << "1부터 " << num << "까지의 합계는 " << sum << std::endl;

		delete[] p;
		save("main.cpp");		// "메인.cpp"를 저장하자
	}
}

====================================================================
저장 시간 - 2026-03-24 14:47:42.0508707
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 24일					(4주 1일)
//------------------------------------------------------------------
// 비주얼 스튜디오의 string 관리 방식 - SSO(Small String Optimization)
// 짧은 문자열은 free store 메모리 할당이 아니라 객체 내부 스택 버퍼에 바로 저장
// 
// 
// 동적 메모리 할당(Dynamic memory allocation)
// 
// Static - 컴파일 타임에 필요한 메모리가 결정됨
// Dynaimc - 런타임에 필요한 메모리가 결정됨
// 
// 메모리는 OS가 관리함
// 컴파일 타임엔 stack, data 구획 사용
// 런타임엔 free-store 구획 사용
// 
// 컴파일 타임에 사용되는 메모리는 이름을 붙여 사용할 수 있다.
// 런타임에 사용되는 메모리는 이름을 붙일 수 없다.
// 이름은 결정된 주소가 있어야 사용 가능함 (free store는 런타임에 결정되어 불가능)
//------------------------------------------------------------------

#include <iostream>
#include <numeric>

#include "save.h"


// [문제] 사용자가 원하는 개수 만큼 int를 저장할 메모리를 확보하라.
// 메모리를 1부터 시작하는 int 값으로 채워라.
// 합계를 계산하여 출력하라.
// 오답(포인터 문제)

int main( )
{
	while ( true ) {
		std::cout << "원하는 만큼의 int 개수 입력: ";
		int num;
		std::cin >> num;

		// 문제없는 수만 입력할거라 희망

		int* p;	// 안 써야할 자료형

		try {
			// contiguous 메모리를 요청함
			p = new int[ num ];
		}
		catch ( std::exception& e ) {
			std::cout << "더 이상 줄 메모리가 없다 - " << e.what( ) << std::endl;
			exit(2022184025);
		}

		std::iota(p , p + num , 1);
		
		long long sum = std::accumulate(p , p + num , 0LL);
		std::cout << "1부터 " << num << "까지의 합계는 " << sum << std::endl;

		delete[] p;
		save("main.cpp");		// "메인.cpp"를 저장하자
	}

}

====================================================================
저장 시간 - 2026-03-24 15:12:12.7917727
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 24일					(4주 1일)
//------------------------------------------------------------------
// 동적 메모리 할당(Dynamic memory allocation)
//------------------------------------------------------------------

#include <iostream>
#include <numeric>
#include <memory>

#include "save.h"


// [문제] 사용자가 원하는 개수 만큼 int를 저장할 메모리를 확보하라.
// 메모리를 1부터 시작하는 int 값으로 채워라.
// 합계를 계산하여 출력하라.
// 정답

int main( )
{
	std::cout << "원하는 만큼의 int 개수 입력: ";
	int num;
	std::cin >> num;

	// 문제없는 수만 입력할거라 희망

	// int* p;	// 안 써야할 자료형
	// C++11(Modern C++)부터 제대로 된 smart pointer가 있으니까

	std::unique_ptr<int[]> p{ new int[ num ] };		// RAII

	std::iota(p.get( ) , p.get( ) + num , 1);

	long long sum = std::accumulate(p.get( ) , p.get( ) + num , 0LL);

	std::cout << "1부터 " << num << "까지의 합계는 " << sum << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-24 23:26:19.2623857
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 24일					(4주 1일)
//------------------------------------------------------------------
// smart pointer - callable type
//------------------------------------------------------------------

#include <iostream>
#include <numeric>
#include <memory>
#include <string>

#include "save.h"


// [문제] 사용자가 원하는 개수 만큼 int를 저장할 메모리를 확보하라.
// 메모리를 1부터 시작하는 int 값으로 채워라.
// 합계를 계산하여 출력하라.
// 발생 가능한 오류 차단 버전

int main( )
{
	std::cout << "원하는 만큼의 int 개수 입력: ";
	std::string input;
	std::getline(std::cin, input);

	int num{ };

	try {
		size_t pos{};
		num = std::stoi(input, &pos);

		if ( pos != input.length( ) ) {
			throw std::invalid_argument("문자가 포함됨");
		}
	}
	catch (...) {
		std::cout << "잘못된 입력 혹은 너무 큰 수 입니다." << std::endl;
		exit(2022184025);
	}

	// int* p;	// 안 써야할 자료형
	// C++11(Modern C++)부터 제대로 된 smart pointer가 있으니까

	try {
		std::unique_ptr<int[]> p{ new int[ num ] };		// RAII

		std::iota(p.get( ) , p.get( ) + num , 1);

		long long sum = std::accumulate(p.get( ) , p.get( ) + num , 0LL);

		std::cout << "1부터 " << num << "까지의 합계는 " << sum << std::endl;
	}
	catch (...) {
		std::cout << "잘못된 개수입니다." << std::endl;
		exit(2022184025);
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// smart pointer - callable type
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main( )
{
	// stack-unwinding 시 p만 삭제됨
	int* p = new int;

	std::cout << *p << std::endl;

	// 여기에서 끝났다. return / exit
	// C++ 언어 표준 - return 문장으로 끝날 때 stack-unwinding 함을 보장함

	return 2022184025;

	std::cout << "이 문장은 절대 출력되지 않음" << std::endl;

	// 메모리 누수 발생
	delete p;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-25 11:48:31.0645764
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// smart pointer - callable type
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Smart {
public:
	Smart(int* p) : p{ p } {
		std::cout << "스마트 생성자 호출됨" << std::endl;
	}
	~Smart( ) {
		std::cout << "스마트 소멸자 호출됨" << std::endl;
		if (p) delete p; 
	}

private:
	int* p;
};

int main( )
{
	save("main.cpp");		// "메인.cpp"를 저장하자

	// 자원의 생명 주기를 p의 생명 주기와 일치
	Smart p{ new int{ 2022184025 } };

	// 여기에서 끝났다. return / exit
	// C++ 언어 표준 - return 문장으로 끝날 때 stack-unwinding 함을 보장함

	return 2022184025;

	std::cout << "이 문장은 절대 출력되지 않음" << std::endl;
}

====================================================================
저장 시간 - 2026-03-25 11:51:34.4672697
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// smart pointer - callable type
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class Dog {
public:
	Dog( ) {
		std::cout << "Dog 생성" << std::endl;
	}
	~Dog( ) {
		std::cout << "Dog 소멸" << std::endl;
	}
};

class Smart {
public:
	Smart(Dog* p) : p{ p } {
		std::cout << "스마트 생성자 호출됨" << std::endl;
	}
	~Smart( ) {
		std::cout << "스마트 소멸자 호출됨" << std::endl;
		if (p) delete p; 
	}

private:
	Dog* p;
};

int main( )
{
	save("main.cpp");		// "메인.cpp"를 저장하자

	// 자원의 생명 주기를 p의 생명 주기와 일치
	Smart p{ new Dog };

	// 여기에서 끝났다. return / exit
	// C++ 언어 표준 - return 문장으로 끝날 때 stack-unwinding 함을 보장함

	return 2022184025;

	std::cout << "이 문장은 절대 출력되지 않음" << std::endl;
}

====================================================================
저장 시간 - 2026-03-25 11:58:45.4537395
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// smart pointer - callable type
//------------------------------------------------------------------

#include <iostream>
#include <memory>

#include "save.h"

class Dog {
public:
	Dog( ) {
		std::cout << "Dog 생성" << std::endl;
	}
	~Dog( ) {
		std::cout << "Dog 소멸" << std::endl;
	}
};

int main( )
{
	std::unique_ptr<Dog[]> p{ new Dog[ 10 ] };
	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-25 12:19:29.4271499
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// callable type - 실습 - STL
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <algorithm>

#include "save.h"

// [문제] "main.cpp"에 있는 글자 중에서 소문자는 모두 대문자로 바꿔(다른 건 그대로)
// "메인대문자.cpp"에 저장하시오

int main( )
{
	std::ifstream in{ "main.cpp" };

	if ( not in ) {
		std::cout << "파일 읽기 오류" << std::endl;
		return 2022184025;
	}

	std::ofstream out{ "메인대문자.cpp" };

	std::transform(std::istreambuf_iterator<char>{in}, {} , std::ostreambuf_iterator<char>{out}, ::toupper);	// toupper - callable type

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-25 12:50:34.2590614
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// callable type - 실습 - STL
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <algorithm>

#include "save.h"

// [문제] "main.cpp"에 있는 글자 중에서 소문자는 모두 대문자로 바꿔(다른 건 그대로)
// "메인대문자.cpp"에 저장하시오

int main( )
{
	std::ifstream in{ "main.cpp" };

	if ( not in ) {
		std::cout << "파일 읽기 오류" << std::endl;
		return 2022184025;
	}

	std::ofstream out{ "메인대문자.cpp" };

	std::transform(std::istreambuf_iterator<char>{in}, {} , std::ostreambuf_iterator<char>{out},
		 []( char c ) {
			c = ::toupper(c);
			return c;
		});	// callable type

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-25 13:18:19.2439154
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 25일					(4주 2일)
//------------------------------------------------------------------
// callable type
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <array>
#include <print>
#include <ranges>

#include "save.h"

// 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// 오름차순 정렬 후 출력하라

std::default_random_engine		dre{};
std::uniform_int_distribution	uid{ 0, 999'9999 };

// a와 a{1} 했을 때 파일 크기 차이나는 이유
std::array<int, 1000'0000> a;

int ascending(const void* a , const void* b) {
	// void* - 사용자가 마음대로 바꿀 수 있는 형식
	// 정렬 함수는 정렬할 타입을 알지만, qsort는 void*로 넘겨주므로 인자가 이렇게 됨
	return *( int* )a - *( int* )b;
}

int main( )
{
	for ( int& num : a ) {
		num = uid(dre);
	}

	std::cout << "정렬 전 - 앞에서부터 1000개만 출력" << std::endl;

	for ( const int& num : a | std::views::take(1000) ) {	// | : 필터. 주어진 데이터를 원하는 방식으로 필터링. 중첩 가능함
		std::print("{:8}" , num);
	}

	// 여기서 qsort를 사용하여 오름차순으로 정렬하라.
	// 전달되는 인자 중 앞의 3개는 물리량, 마지막은 코드이다.
	std::qsort(a.data( ) , a.size( ) , sizeof(std::array<int, 1000'0000>::value_type) , ascending);

	std::cout << "정렬 후 - 앞에서부터 1000개만 출력" << std::endl;

	for ( const int& num : a | std::views::take(1000) ) {	// | : 필터. 주어진 데이터를 원하는 방식으로 필터링. 중첩 가능함
		std::print("{:8}" , num);
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 13:39:58.5630899
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <array>
#include <chrono>

#include "save.h"

// 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// 오름차순 정렬 후 출력하라

std::default_random_engine		dre{};
std::uniform_int_distribution	uid{ 0, 999'9999 };

// a와 a{1} 했을 때 파일 크기 차이나는 이유
std::array<int, 1000'0000> a;

int ascending(const void* a , const void* b) {
	// void* - 사용자가 마음대로 바꿀 수 있는 형식
	// 정렬 함수는 정렬할 타입을 알지만, qsort는 void*로 넘겨주므로 인자가 이렇게 됨
	return *( int* )a - *( int* )b;
}

int main( )
{
	for ( int& num : a ) {
		num = uid(dre);
	}

	// 여기서 qsort를 사용하여 오름차순으로 정렬하라.
	// 전달되는 인자 중 앞의 3개는 물리량, 마지막은 코드이다.

	auto start = std::chrono::high_resolution_clock::now( );	// 시간재기 시작
	std::qsort(a.data( ) , a.size( ) , sizeof(int) , ascending);
	auto stop = std::chrono::high_resolution_clock::now( );		// 시간 끝
	// std::cout << "qsort에 걸린 시간" << stop - start << std::endl;

	std::cout << "qsort에 걸린 시간" << std::chrono::duration_cast< std::chrono::milliseconds >( stop - start ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 13:59:09.7850806
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>

#include "save.h"

// 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// 오름차순 정렬 후 출력하라

std::default_random_engine		dre{};
std::uniform_int_distribution	uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int ascending(const void* a , const void* b)
{
	return *( int* )a - *( int* )b;
}

bool sort용ascending(int a, int b)
{
	// a = b인 경우는 stable sort를 위해 사용
	return a < b;
}


int main( )
{
	// qsort로 천만개 정렬하는데 걸린 시간
	{
		for ( int& num : a ) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now( );
		std::qsort(a.data( ) , a.size( ) , sizeof(int) , ascending);
		auto stop = std::chrono::high_resolution_clock::now( );

		std::cout << "qsort - " << std::chrono::duration_cast< std::chrono::milliseconds >( stop - start ) << std::endl;
	}

	// std::sort로 천만개 정렬하는데 걸린 시간
	{
		for ( int& num : a ) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now( );
		std::sort(a.begin() , a.end(), sort용ascending);
		auto stop = std::chrono::high_resolution_clock::now( );

		std::cout << "sort - " << std::chrono::duration_cast< std::chrono::milliseconds >( stop - start ) << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 14:05:01.0869475
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <array>
#include <chrono>
#include <algorithm>

#include "save.h"

// 랜덤값을 갖는 int 1000만개를 메모리에 저장하라.
// 오름차순 정렬 후 출력하라

std::default_random_engine		dre{};
std::uniform_int_distribution	uid{ 0, 999'9999 };

std::array<int, 1000'0000> a;

int ascending(const void* a , const void* b)
{
	return *( int* )a - *( int* )b;
}

bool sort용ascending(int a, int b)
{
	// a = b인 경우는 stable sort를 위해 사용
	return a < b;
}


int main( )
{
	// qsort로 천만개 정렬하는데 걸린 시간
	{
		for ( int& num : a ) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now( );
		std::qsort(a.data( ) , a.size( ) , sizeof(int) , ascending);
		auto stop = std::chrono::high_resolution_clock::now( );

		std::cout << "qsort - " << std::chrono::duration_cast< std::chrono::milliseconds >( stop - start ) << std::endl;
	}

	// std::sort로 천만개 정렬하는데 걸린 시간
	{
		for ( int& num : a ) {
			num = uid(dre);
		}

		auto start = std::chrono::high_resolution_clock::now( );
		std::sort(a.begin( ) , a.end( ) , [](int a, int b) {
			return a < b;
			});
		auto stop = std::chrono::high_resolution_clock::now( );

		std::cout << "sort - " << std::chrono::duration_cast< std::chrono::milliseconds >( stop - start ) << std::endl;
	}

	// 속도 차이의 이유
	// 
	// qsort: DATA segment의 데이터를 CODE segment의 qsort->ascending으로 전달 후 비교 결과를 리턴
	// ascending에 인자를 전달하는 과정에서 Overhead 발생. STACK segment 사용 및 함수 리턴 시 복귀를 위해 추가적인 비용 발생(스택 프레임 등)
	// 위 과정이 NlogN 걸림
	// 
	// sort: DATA segment의 데이터를 가져오는 것은 동일하나, CODE segment에서 sort 자체적으로 람다 함수를 통해
	// 오버헤드 없이 빠른 비교 가능.

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 14:19:29.1215673
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

unsigned cnt{};

int 오름(const void* a , const void* b)
{
	std::cout << "비교함수 - " << *(int*)a << " - " << *( int* )b << std::endl;
	++cnt;
	return *( int* )a - *( int* )b;
}

int main( )
{
	int a[]{ 9, 2, 5, 1, 8, 3, 4, 6, 7, 0 };	// qsort 시: 10 * log10 = 약 40회

	std::qsort(a , 10 , 4 , 오름);

	std::cout << "비교 함수 호출 - " << cnt << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 14:21:24.0413895
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>
#include <algorithm>

#include "save.h"

unsigned cnt{};

bool 오름(int a , int b)
{
	++cnt;
	return a < b;
}

int main( )
{
	int a[]{ 9, 2, 5, 1, 8, 3, 4, 6, 7, 0 };	// sort 시: 27?

	std::sort(a, a + 10 , 오름);

	std::cout << "비교 함수 호출 - " << cnt << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 14:22:27.3177036
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
//------------------------------------------------------------------

#include <iostream>
#include <algorithm>

#include "save.h"

unsigned cnt{};

int main( )
{
	int a[]{ 9, 2, 5, 1, 8, 3, 4, 6, 7, 0 };	// sort 시: 27?

	std::sort(a , a + 10 , [](int a , int b) {
		++cnt;
		return a < b;
		});

	// sort는 템플릿 알고리즘 함수이다. Random Iterator에 대해 템플릿 특수화 진행
	// Iterator의 타입을 먼저 살펴본다.
	// 이후 특수화 과정에서, 비교 함수로 적힌 람다 함수를 보고 특수화 할 때 코드를 통째로 적는다.
	// 이를 in-line이라고 한다.

	std::cout << "비교 함수 호출 - " << cnt << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 14:55:20.1138895
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

void f( )
{
	std::cout << "난 호출가능합니다" << std::endl;
}

int main( )
{
	int n;		// sizeof, addressof, typeid
	std::cout << typeid( n ).name( ) << std::endl;
	int a = n;

	std::cout << typeid( f ).name() << std::endl;
	void (*b)() = f;

	b( );

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:03:24.7191491
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

void Jump( )
{
	std::cout << "점프" << std::endl;
}

void Slide( )
{
	std::cout << "슬라이드" << std::endl;
}

int main( )
{
	void (*일번키)() = Jump;

	일번키( );
	일번키( );

	일번키 = Slide;

	일번키( );
	일번키( );

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:06:11.8114177
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main( )
{
	// 이름없는 함수
	[]( ) {
		std::cout << "안녕 난 람다야" << std::endl;
		};

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:06:42.7334268
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main( )
{
	// 이름없는 함수
	[]( ) {
		std::cout << "안녕 난 람다야" << std::endl;
		} ();

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:07:42.4188821
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main( )
{
	// 이름없는 함수
	std::cout << typeid([]( ) {
		std::cout << "안녕 난 람다야" << std::endl;
		}).name( ) << std::endl;
	
	// class `int __cdecl main(void)'::`2'::<lambda_1>
	// 클래스 /int리턴    /main(void)내부의

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:13:08.4796180
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

int main( )
{

	class XYZ {
	public:
		void operator()( ) {
			std::cout << "안녕 난 람다야" << std::endl;
		}
	};

	XYZ a;
	a( );
	std::cout << typeid( a ).name( ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-03-31 15:14:00.8049562
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				3월 31일					(5주 1일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
// 3. 함수 호출 연산자를 오버로딩 한 클래스 - functor
//------------------------------------------------------------------

#include <iostream>

#include "save.h"

class XYZ {
public:
	void operator()( ) {
		std::cout << "안녕 난 람다야" << std::endl;
	}
};

int main( )
{
	XYZ a;
	a( );
	std::cout << typeid( a ).name( ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 11:36:48.1242774
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
// 3. 함수 호출 연산자를 오버로딩 한 클래스
// 4. 멤버 함수 포인터
// 
// 통합하여 function 클래스로 사용
//------------------------------------------------------------------

#include <iostream>
#include <array>
#include <algorithm>

#include "save.h"

class Dog {
public:
	bool operator()(int a, int b) {
		return a > b;
	}
};

int main( )
{
	std::array<int , 10> a{ 3, 0, 7, 1, 2, 8, 9, 6, 5, 4 };

	// ascending order
	std::sort(a.begin( ) , a.end( ) , Dog{});

	for ( int num : a )
		std::cout << num << ' ';
	std::cout << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 11:45:59.6701082
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 호출가능한 타입 - callable type
// 1. 함수
// 2. lambda - 이름 없는 함수
// 3. 함수 호출 연산자를 오버로딩 한 클래스
// 4. 멤버 함수 포인터
// 
// 통합하여 function 클래스로 사용
//------------------------------------------------------------------

#include <iostream>
#include <array>
#include <algorithm>
#include <functional>

#include "save.h"

bool 내림(int a , int b) 
{
	return a > b;
}

class Dog {
public:
	bool operator()(int a, int b) {
		return a > b;
	}
};

int main( )
{
	std::array<int , 10> a{ 3, 0, 7, 1, 2, 8, 9, 6, 5, 4 };

	// 리턴값과 인자를 템플릿으로 받음
	// std::function은 callable type을 가리킬 수 있음
	std::function<bool(int , int)> f = 내림;
	f = Dog{};
	f = [](int a, int b) {
			return a < b;
		};

	// ascending order
	std::sort(a.begin( ) , a.end( ) , f);

	for ( int num : a )
		std::cout << num << ' ';
	std::cout << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 12:08:57.4854192
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 16 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

int main( )
{
	for ( int i = 0; i < 10; ++i ) {
		std::cout << Dog{} << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 12:38:05.9632229
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <array>
#include <algorithm>
#include <ranges>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 16 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	size_t getID( ) const {
		return id;
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

// [문제] 1000만개의 Dog 객체를 메모리에 저장하라.
// sort를 사용하여 id 기준으로 오름차순 정렬하라.
// 정렬한 객체를 앞에서부터 100개를 화면에 출력하라
// - 필요한 interface 함수를 Dog에 추가해도 좋다.

std::array<Dog , 1000'0000> arr;

int main( )
{
	std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getID() < b.getID();
		});

	for ( const Dog& dog : arr | std::views::take(100) ) {
		std::cout << dog << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 12:44:00.8387497
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <array>
#include <algorithm>
#include <ranges>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 16 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	size_t getID( ) const {
		return id;
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

// [문제] 1000만개의 Dog 객체를 메모리에 저장하라.
// sort를 사용하여 id 기준으로 오름차순 정렬하라.
// 정렬한 객체를 앞에서부터 100개를 화면에 출력하라
// - 필요한 interface 함수를 Dog에 추가해도 좋다.

std::array<Dog , 1000'0000> arr;

int main( )
{
	/*std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getID() < b.getID();
		});*/

	std::ranges::sort(arr , {}, &Dog::getID);	// 마지막 인자는 callable이어야 함 -> 포인터 사용

	for ( const Dog& dog : arr | std::views::take(100) ) {
		std::cout << dog << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 12:52:45.1287608
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <array>
#include <algorithm>
#include <ranges>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 160 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	size_t getID( ) const {
		return id;
	}

	std::string getName( ) const {
		return name;
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

// [문제] 1000만개의 Dog 객체를 메모리에 저장하라.
// sort를 사용하여 name의 길이를 기준으로 오름차순 정렬하라.
// 정렬한 객체를 앞에서부터 10000개를 화면에 출력하라
// - 필요한 interface 함수를 Dog에 추가해도 좋다.

std::array<Dog , 1000'0000> arr;

int main( )
{
	/*std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getID() < b.getID();
		});*/

	std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getName().size() < b.getName().size();
		});

	// C++20
	// std::ranges::sort(arr , {}, &Dog::getID);	// 마지막 인자는 callable이어야 함 -> 포인터 사용

	for ( const Dog& dog : arr | std::views::take(10000) ) {
		std::cout << dog << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 13:02:01.4283606
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <array>
#include <algorithm>
#include <ranges>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 160 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	size_t getID( ) const {
		return id;
	}

	std::string getName( ) const {
		return name;
	}

	std::string& getName( ) {
		return name;
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

// [문제] 1000만개의 Dog 객체를 메모리에 저장하라.
// sort를 사용하여 name을 알파벳 기준 오름차순으로 정렬하라.
// 정렬한 객체를 앞에서부터 10000개를 화면에 출력하라
// - 필요한 interface 함수를 Dog에 추가해도 좋다.

std::array<Dog , 1000'0000> arr;

int main( )
{
	/*std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getID() < b.getID();
		});*/

	for ( Dog& dog : arr ) {
		std::string& name = dog.getName( );
		std::sort(name.begin( ) , name.end( ));
	}

	std::sort(arr.begin( ) , arr.end( ) , [](const Dog& a , const Dog& b) {
		return a.getName().size() < b.getName().size();
		});

	// C++20
	// std::ranges::sort(arr , {}, &Dog::getID);	// 마지막 인자는 callable이어야 함 -> 포인터 사용

	for ( const Dog& dog : arr | std::views::take(1000) ) {
		std::cout << dog << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-01 13:06:50.1818711
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 1일					(5주 2일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------

#include <iostream>
#include <random>
#include <print>
#include <array>
#include <algorithm>
#include <ranges>
#include <fstream>

#include "save.h"

std::default_random_engine dre;
std::uniform_int_distribution uidID { 1, 999'9999 };
std::uniform_int_distribution uidLen{ 1, 16 };
std::uniform_int_distribution uidChar{ 33, 122 };

class Dog {
public:
	Dog( ) {
		id = uidID(dre);
		size_t len = uidLen(dre);
		for ( int i = 0; i < len; ++i ) {
			name += uidChar(dre);
		}
	}

	size_t getID( ) const {
		return id;
	}

	std::string getName( ) const {
		return name;
	}

	std::string& getName( ) {
		return name;
	}

	friend std::ostream& operator<<(std::ostream& os , const Dog& dog) {
		std::print(os , "[{:7}] {}" , dog.id , dog.name);
		return os;
	}
private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]
};

// [문제] 1000만개의 Dog 객체를 메모리에 저장하라.
// 바이너리 파일 "Dog천만마리"에 write함수를 사용하여 각 Dog 객체를 모두 저장하였다.
// 

std::array<Dog , 1000'0000> dogs;

int main( )
{
	std::ofstream out{ "Dog천만마리", std::ios::binary };

	out.write((char*)( dogs.data( ) ) , dogs.size( ) * sizeof(Dog));
	
	save("main.cpp");		// "메인.cpp"를 저장하자
}

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// 연습
//------------------------------------------------------------------
// 아래 코드는 돌아가는 코드가 아님!!!!!!!
// string은 16문자부턴 free store 공간에 문자열을 저장하고 객체 자체는 그 포인터를 가짐

#include <iostream>
#include <fstream>
#include <array>
#include <ranges>

#include "save.h"

class Dog {
public:
	void show( ) const {
		std::cout << "이름 - " << name << ", id - " << id << std::endl;
	}

	std::string getName( ) const { return name; }

private:
	std::string name;	// [1, 16]
	size_t		id;		// [1, 999'9999]

};

// [문제] 바이너리 파일 "Dog천만마리"에는 write로 Dog 천만 객체를 기록하였다.
// array에 모든 객체를 읽어와라.
// 파일 크기는 1000만 * sizeof(Dog) 이다 -> 400,000,000
// 앞에서 100개를 화면에 출력하라

// 전역 객체는 반드시 생성자가 호출된다
std::array<Dog , 1000'0000> dogs;

// 전역 객체의 단 하나의 요소라도 명시적으로 초기화를 하면, 실행파일의 크기가 많이 차이나게 된다.
// std::array<Dog , 1000'0000> dogs{ 10 };

int main( )
{
	// stack에 생성된 객체는 생성자 호출을 보장하지 않는다. {}로 초기화한다.
	// std::array<Dog , 10> dogs{};

	std::ifstream in{ "Dog천만마리", std::ios::binary };
	if ( not in ) {
		std::cout << "파일에 문제가 있음" << std::endl;
		return 2022184025;
	}

	in.read(( char* )dogs.data( ) , dogs.size() * sizeof(Dog));

	for ( const Dog& dog : dogs | std::views::take(100)) {
		std::cout << dog.getName( ).size( ) << std::endl;
	}

	//save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-07 14:40:07.2022627
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <string>

#include "save.h"

int main( )
{
	std::string s{ "0123456789" };
	for ( const char& c : s ) {
		std::cout << c << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-07 14:41:11.3446981
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <string>

#include "save.h"

int main( )
{
	std::string s{ "0123456789" };
	for ( auto i = s.begin( ); i < s.end( ); ++i ) {
		std::cout << *i << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-07 14:42:22.5414278
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <string>

#include "save.h"

int main( )
{
	std::string s{ "0123456789" };
	
	s += "abcdefghijklmnopqrstuvwxyz";

	std::cout << s << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-07 15:02:10.2115458
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <memory>

#include "save.h"

class YString {
public:
	YString( ) = default;

	YString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get( ) , s , len);
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};

	friend std::ostream& operator<<(std::ostream& os , const YString& ys) {
		for ( int i = 0; i < ys.len; ++i ) {
			os << *( ys.p.get( ) + i );
		}
		return os;
	}
};

int main( )
{
	YString s{ "0123456789" };
	
	std::cout << s << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-07 15:05:17.4734340
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <memory>

#include "save.h"

class YString {
public:
	YString( ) = default;

	YString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get( ) , s , len);
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};

	friend std::ostream& operator<<(std::ostream& os , const YString& ys) {
		for ( int i = 0; i < ys.len; ++i ) {
			os << *( ys.p.get( ) + i );
		}
		return os;
	}
};

int main( )
{
	std::string s{ "2026년 4월 7일" };
	std::string t = move(s);

	// 아래는 반칙 코드이다.
	// s는 xvalue(expired value)인데, 이를 cout으로 호출하는 것은
	// undefined behaviour, 즉 실행 결과가 어떻게 되든 보장하지 않는다.
	// 자세한 내용은 교과서의 value - category
	std::cout << "s - " << s << std::endl;
	std::cout << "t - " << t << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-07 15:18:07.3643156
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 7일					(6주 1일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <memory>

#include "save.h"

class YString {
public:
	YString( ) = default;

	YString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get( ) , s , len);

		std::cout << "생성(char*) 글자수: " << len << ", 주소: " << this << ", 글자주소:" << (void*)p.get() << std::endl;
	}

private:
	size_t len{};
	std::unique_ptr<char[]> p{};

	friend std::ostream& operator<<(std::ostream& os , const YString& ys) {
		for ( int i = 0; i < ys.len; ++i ) {
			os << *( ys.p.get( ) + i );
		}
		return os;
	}
};

int main( )
{
	YString s{ "2026년 4월 7일" };

	std::cout << "s - " << s << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-08 11:55:14.5574486
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 8일					(6주 2일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <memory>
#include <print>

#include "save.h"

class YString {
public:
	YString( ) {
		debugPrint("생성()");
	}

	// 이 소멸자는 만들 이유가 전혀 없으나 관찰용으로 만듦.
	~YString( ) {
		debugPrint("소멸()");
	}

	// 이건 스페셜 함수가 아니다
	YString(const char* s) {
		len = std::strlen(s);
		p = std::make_unique<char[]>(len);
		memcpy(p.get( ) , s , len);

		debugPrint("생성(*)");
	}

private:
	size_t id{};
	size_t len{};
	std::unique_ptr<char[]> p{};

	friend std::ostream& operator<<(std::ostream& os , const YString& ys) {
		for ( int i = 0; i < ys.len; ++i ) {
			os << *( ys.p.get( ) + i );
		}
		return os;
	}

	void debugPrint(const char* type) const {
		std::string 글자;
		size_t printNum = 10;
		if ( len < printNum )
			printNum = len;
		for ( int i = 0; i < len; ++i )
			글자 += p.get( )[ i ];
		std::println("[{:8}] {:8} - 객체:{:#016X} 글자:{:#016X} 개수:{:<4} 내용:{}" ,
			id , type , ( unsigned long long )this , ( unsigned long long )p.get( ) , len , 글자);
	}
};

int main( )
{
	// 아래 이름 없는 객체는, 디버깅 메시지를 디폴트 생성자에 넣지 않았더라면 컴파일러의 최적화에 의해 생성조차 되지 않는다.
	YString{};
	YString s{ "2026년 4월 7일" };

	std::cout << "s - " << s << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-08 12:16:19.7796164
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 8일					(6주 2일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;

	YString a[ 5 ]{ "333", "1", "55555", "22", "4444" };

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-08 12:51:12.4420468
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 8일					(6주 2일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;

	YString s{ "2026년 4월 8일" };

	YString t = s;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-08 12:54:33.9104183
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 8일					(6주 2일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{

	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	// 아래 코드는 쓰레기 코드이다 (불필요한 복사 발생).
	observe = true;
	for ( YString s : a ) {
		std::cout << s << std::endl;
	}
	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}


====================================================================
저장 시간 - 2026-04-08 13:03:02.8954715
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 8일					(6주 2일)
//------------------------------------------------------------------
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <array>
#include <algorithm>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{

	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	observe = true;

	// 오름차순으로 정렬하라
	// 아래 정렬 코드는 쓰레기 코드이다. const YString& 하자
	std::sort(a.begin( ) , a.end( ) , [](YString a, YString b) {
		return a.getLen( ) < b.getLen( );
		});
	observe = false;

	for ( YString& s : a ) {
		std::cout << s << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}


//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// 중간고사 4월 22일 3 4교시
// STL 컨테이너 - std::string을 코딩하여 STL 컨테이너에 필요한 것들을 알아본다
//------------------------------------------------------------------

#include <iostream>
#include <array>
#include <algorithm>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{

	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	observe = true;

	// 오름차순으로 정렬하라
	// 복사와 이동의 속도 차이를 구하는 것이 문제로 출제될 수 있음
	std::sort(a.begin( ) , a.end( ) , [](const YString& a, const YString& b) {
		return a.getLen( ) < b.getLen( );
		});
	observe = false;

	for ( YString& s : a ) {
		std::cout << s << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

/*
이동생성자가 없는 경우의 정렬
[       6] 복사생성 - 객체:0X00004172AFF720 글자:0X00016AF1133200 개수:1    내용:1
[       2] 복사할당 - 객체:0X00004172AFF808 글자:0X00016AF11332F0 개수:3    내용:333
[       1] 복사할당 - 객체:0X00004172AFF7F0 글자:0X00016AF1133210 개수:1    내용:1
[       6] 소멸     - 객체:0X00004172AFF720 글자:0X00016AF1133200 개수:1    내용:1
[       7] 복사생성 - 객체:0X00004172AFF720 글자:0X00016AF1133220 개수:5    내용:55555
[       3] 복사할당 - 객체:0X00004172AFF820 글자:0X00016AF1133150 개수:5    내용:55555
[       7] 소멸     - 객체:0X00004172AFF720 글자:0X00016AF1133220 개수:5    내용:55555
[       8] 복사생성 - 객체:0X00004172AFF720 글자:0X00016AF1133170 개수:4    내용:4444
[       4] 복사할당 - 객체:0X00004172AFF838 글자:0X00016AF1133160 개수:5    내용:55555
[       3] 복사할당 - 객체:0X00004172AFF820 글자:0X00016AF1133180 개수:4    내용:4444
[       8] 소멸     - 객체:0X00004172AFF720 글자:0X00016AF1133170 개수:4    내용:4444
[       9] 복사생성 - 객체:0X00004172AFF720 글자:0X00016AF11331F0 개수:2    내용:22
[       5] 복사할당 - 객체:0X00004172AFF850 글자:0X00016AF1133200 개수:5    내용:55555
[       4] 복사할당 - 객체:0X00004172AFF838 글자:0X00016AF1133190 개수:4    내용:4444
[       3] 복사할당 - 객체:0X00004172AFF820 글자:0X00016AF1133150 개수:3    내용:333
[       2] 복사할당 - 객체:0X00004172AFF808 글자:0X00016AF1133220 개수:2    내용:22
[       9] 소멸     - 객체:0X00004172AFF720 글자:0X00016AF11331F0 개수:2    내용:22
1
22
333
4444
55555

이동
[       2] 이동생성 - 객체:0X00002DAB0FF870 글자:0X0001D7CD6225E0 개수:1    내용:1
[       2] 이동할당 - 객체:0X00002DAB0FF9F8 글자:0X0001D7CD6225A0 개수:3    내용:333
[       1] 이동할당 - 객체:0X00002DAB0FF9E0 글자:0X0001D7CD6225E0 개수:1    내용:1
[       2] 소멸     - 객체:0X00002DAB0FF870 글자:0X00000000000000 개수:0    내용:
[       3] 이동생성 - 객체:0X00002DAB0FF870 글자:0X0001D7CD6226B0 개수:5    내용:55555
[       3] 이동할당 - 객체:0X00002DAB0FFA10 글자:0X0001D7CD6226B0 개수:5    내용:55555
[       3] 소멸     - 객체:0X00002DAB0FF870 글자:0X00000000000000 개수:0    내용:
[       4] 이동생성 - 객체:0X00002DAB0FF870 글자:0X0001D7CD622690 개수:4    내용:4444
[       4] 이동할당 - 객체:0X00002DAB0FFA28 글자:0X0001D7CD6226B0 개수:5    내용:55555
[       3] 이동할당 - 객체:0X00002DAB0FFA10 글자:0X0001D7CD622690 개수:4    내용:4444
[       4] 소멸     - 객체:0X00002DAB0FF870 글자:0X00000000000000 개수:0    내용:
[       5] 이동생성 - 객체:0X00002DAB0FF870 글자:0X0001D7CD622610 개수:2    내용:22
[       5] 이동할당 - 객체:0X00002DAB0FFA40 글자:0X0001D7CD6226B0 개수:5    내용:55555
[       4] 이동할당 - 객체:0X00002DAB0FFA28 글자:0X0001D7CD622690 개수:4    내용:4444
[       3] 이동할당 - 객체:0X00002DAB0FFA10 글자:0X0001D7CD6225A0 개수:3    내용:333
[       2] 이동할당 - 객체:0X00002DAB0FF9F8 글자:0X0001D7CD622610 개수:2    내용:22
[       5] 소멸     - 객체:0X00002DAB0FF870 글자:0X00000000000000 개수:0    내용:
1
22
333
4444
55555
*/

====================================================================
저장 시간 - 2026-04-14 15:00:07.9667064
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
// 
// object - "클래스로 생성한(Instancing)" 메모리에 존재하는 것
// homogeneous(서로같음)한 데이터 타입에 대해서만 담을 수 있다.
// heterogeneous(서로다른)한 데이터 타입은 허용하지 않는다
// 
// 컨테이너는 세 그룹이 있다.
// 1(Sequential): array<T, n>, deque<T>, forward_list<T>, list<T>, vector<T>
// ㄴ 특정 메모리 위치를 맘대로 액세스 가능하다.
// 2(Associative). set, map, multiset, multimap
// ㄴ 맘대로 메모리 위치 액세스 불가, 빠르게 찾기에 특화 O(log N)
// 3(Unordered ''). unordered_set, unordered_map, unordered_multi~~ 2개
// ㄴ 액세스 불가는 동일, 속도는 더 빠름 O(1)
// array를 제외하고 전부 Dynamic memory(free store 관리)이다.
// 
// array만 structure인 이유 : 멤버 변수를 감출 이유가 전혀 없기 때문이다.
// 일반 자료구조를 포장한 것에 불과하다.
// dynamic memory가 아니므로, iterator가 invalidation 되지 않는다.
// 다만 array 간 swap 실행 시 iterator는 동일 위치를 가리키므로 값이 바뀌어 나온다.
// array의 객체 타입은 이동 생성과 할당이 가능하여야 한다.
// 즉 T u = rv;와 T(rv)가 가능해야 함. rv는 우측값 rvalue
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	// i++인 경우 복사 생성되므로 비효율적
	for ( auto i = a.begin( ); i != a.end( ); ++i ) {
		std::cout << *i << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-14 15:01:54.3354993
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>
#include <list>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;
	std::list<YString> a{ "333", "1", "55555", "4444", "22" };

	// i++인 경우 복사 생성되므로 비효율적
	for ( auto i = a.begin( ); i != a.end( ); ++i ) {
		std::cout << *i << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-14 15:02:29.7411302
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };
	a.fill("2026년 4월 14일");

	// i++인 경우 복사 생성되므로 비효율적
	for ( auto i = a.begin( ); i != a.end( ); ++i ) {
		std::cout << *i << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-14 15:03:30.3232324
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	// i++인 경우 복사 생성되므로 비효율적
	for ( auto i = a.rbegin( ); i != a.rend( ); ++i ) {
		std::cout << *i << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-14 15:12:00.5878251
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	std::cout << "원소 개수 - " << a.size( ) << std::endl;
	std::cout << "비어있니? - " << std::boolalpha << a.empty( ) << std::endl;
	std::cout << "최대로 담는 원소 개수 - " << a.max_size() << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-14 15:18:06.2360919
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 14일					(7주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<YString , 5> a{ "333", "1", "55555", "4444", "22" };

	// element access
	// at, operator[], front, back, data
	std::cout << "첫	 원소 - " << a.front( ) << std::endl;
	std::cout << "마지막 원소 - " << a.back( ) << std::endl;
	std::cout << "첫	 원소 - " << *(a.data() + sizeof(YString) * 0) << std::endl;
	std::cout << "마지막 원소 - " << a[a.size() - 1] << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N> - 유일하게 크기 고정된 자료구조
//------------------------------------------------------------------

#include <iostream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::array<int , 5> a{ 1, 2, 3, 4, 5 };

	// element access
	// at, operator[], front, back, data

	while ( true ) {
		std::cout << "몇 번째 값을 알려줄까요? ";
		int num;
		std::cin >> num;

		try {
			// at() - 값 검사를 하려는 의도가 있다면
			// []에 해당 기능이 없는 이유 - 함수 호출 속도까지도 줄이기 위함
			std::cout << num << " 번째 값은 - " << a.at(num) << std::endl;
		}
		catch (std::exception e) {
			std::cout << e.what( ) << "- 유효 범위는 [0, 4]" << std::endl;
		}
	}

	//save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-15 12:04:47.8849417
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<int> v{ 1, 2, 3, 4, 5, };

	std::cout << "v의 크기 - " << sizeof( v ) << std::endl;
	std::cout << "v의 주소 - " << std::addressof(v) << std::endl;
	std::cout << "v의 타입 - " << typeid( v ).name( ) << std::endl;
	std::cout << "v의 자료 - " << v.data() << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-15 12:05:21.8434241
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

std::vector<int> v{ 1, 2, 3, 4, 5, };

int main( )
{

	std::cout << "v의 크기 - " << sizeof( v ) << std::endl;
	std::cout << "v의 주소 - " << std::addressof(v) << std::endl;
	std::cout << "v의 타입 - " << typeid( v ).name( ) << std::endl;
	std::cout << "v의 자료 - " << v.data() << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-15 12:08:36.7070373
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<int> v{ 1, 2, 3, 4, 5, };

	// 핵심 벡터 정보들
	std::cout << "v에 저장된 원소 수 - " << v.size( ) << std::endl;
	std::cout << "v가 확보한 capacity - " << v.capacity( ) << std::endl;
	std::cout << "v의 원소가 저장된 주소 - " << v.data( ) << std::endl;

	v.push_back(6);

	std::cout << "원소 추가 후" << std::endl;
	std::cout << "v에 저장된 원소 수 - " << v.size( ) << std::endl;
	std::cout << "v가 확보한 capacity - " << v.capacity( ) << std::endl;
	std::cout << "v의 원소가 저장된 주소 - " << v.data( ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-15 12:32:40.8666238
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	class YYY {
	};
	std::vector<int> v{ 1, 2, 3, 4, 5, };

	std::cout << "벡터가 담을 수 있는 최대 YYY 개수" << v.max_size( ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자 
}

====================================================================
저장 시간 - 2026-04-15 12:45:53.8994333
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <fstream>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	// [문제] "main.cpp" 에 있는 글자를 모두 읽어 메모리에 저장하라.
	std::vector<char> v;
	std::ifstream in{ "main.cpp" };

	if ( not in ) {
		std::cout << "파일 문제 발생!" << std::endl;
		return 2022184025;
	}

	// 원소 추가는 v.push_back(한글자);
	char c;
	in >> std::noskipws;
	while ( in >> c ) {
		v.push_back(c);
	}

	for ( auto i = v.begin( ); i != v.end( ); ++i ) {
		std::cout << *i;
	}

	/*for ( char c : v ) {
		std::cout << c;
	}*/

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-15 12:47:31.1046846
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <fstream>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	// [문제] "main.cpp" 에 있는 글자를 모두 읽어 메모리에 저장하라.
	std::ifstream in{ "main.cpp" };

	if ( not in ) {
		std::cout << "파일 문제 발생!" << std::endl;
		return 2022184025;
	}

	std::vector<char> v{ std::istreambuf_iterator(in), {} };

	for ( auto i = v.begin( ); i != v.end( ); ++i ) {
		std::cout << *i;
	}

	/*for ( char c : v ) {
		std::cout << c;
	}*/

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-15 13:07:59.1511022
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	// [문제] 키보드에서 입력한 단어를 메모리에 저장하라.
	// 오름차순으로 정렬하라.
	// 화면에 출력하라.
	// 키보드 입력의 끝 - Ctrl + Z

	std::vector<std::string> words;

	std::string word;

	std::cout << "원하는 만큼 단어를 입력하세요. 입력 종료는 Ctrl + Z" << std::endl;
	while ( std::cin >> word ) {
		words.push_back(word);
	}
	std::cout << "\n입력 종료 =======================\n" << std::endl;

	std::sort(words.begin( ) , words.end( ));

	for ( const std::string& word : words ) {
		std::cout << word << std::endl;
	}

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-15 13:10:50.7806258
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	// [문제] 키보드에서 입력한 단어를 메모리에 저장하라.
	// 오름차순으로 정렬하라.
	// 화면에 출력하라.
	// 키보드 입력의 끝 - Ctrl + Z

	std::cout << "원하는 만큼 단어를 입력하세요. 입력 종료는 Ctrl + Z" << std::endl;
	std::vector<std::string> words{ std::istream_iterator<std::string>{std::cin }, {} };


	std::cout << "\n입력 종료 =======================\n" << std::endl;

	std::sort(words.begin( ) , words.end( ));

	std::copy(words.begin( ) , words.end( ) , std::ostream_iterator<std::string>{ std::cout , "\n"});

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-15 13:18:50.2360094
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 15일					(7주 2일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	// [문제] 키보드에서 입력한 정수의 합을 출력하라

	std::cout << "수 입력: " << std::accumulate(std::istream_iterator<int>{std::cin} , {} , 0);

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 13:34:32.6682559
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;
	YString ys{ "2026년 4월 21일 화요일 56교시" };
	std::cout << ys << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 13:47:39.3490593
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	YString ys{ "2026년 4월 21일 화요일 56교시" };
	std::cout << "컨테이너의 원소 개수 - " << ys.size() << std::endl;
	std::cout << "데이터는 어디에?     - " << (void*)ys.data() << std::endl;

	observe = true;
	ys.show();
	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 13:52:07.7147877
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<YString> v{ "2026년", "4월", "21일" };

	for ( YString& ys : v ) {
		ys.show( );
	}
	

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 13:59:15.4187480
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<int> v;

	std::cout << "v의 원소 개수   - " << v.size( ) << std::endl;
	std::cout << "v의 용량        - " << v.capacity( ) << std::endl;
	std::cout << "v가 저장한 위치 - " << v.data( ) << std::endl;

	v.push_back(1);

	std::cout << std::endl;
	std::cout << "원소 한 개 추가" << std::endl;
	std::cout << "v의 원소 개수   - " << v.size( ) << std::endl;
	std::cout << "v의 용량        - " << v.capacity( ) << std::endl;
	std::cout << "v가 저장한 위치 - " << v.data( ) << std::endl;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<int> v;

	// VS는 벡터 용량을 1.5배씩 늘린다. free stor에.
	// 시험에서 heap 용어를 쓰거나 배우지 않은 내용 적으면 무조건 감점
	// 메모리 그림으로 한 번은 그려보자
	while ( true ) {
		if ( v.size( ) == v.capacity( ) ) {
			std::cout << "v의 원소 개수   - " << v.size( ) << std::endl;
			std::cout << "v의 용량        - " << v.capacity( ) << std::endl;
			std::cout << "v가 저장한 위치 - " << v.data( ) << std::endl;
			std::cout << std::endl;
		}
		v.push_back(1);
	}

	//save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 14:50:38.4766923
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;
	std::vector<YString> v;

	// v에 담을 공간이 없으므로(capacity = 0)
	// free-store에 공간 하나 할당
	// "2026년"을 갖는 YString instance를 Stack에 생성자로 생성
	// 만들어진 instance를 벡터가 관리하는 free-store에 이동생성
	// (우리가 이동 생성 정의한 대로 생성됨)
	// push_back 종료, 기존 instance 소멸
	v.push_back("2026년");
	std::cout << std::endl;

	// v에 담을 공간이 없으므로(capacity = 1)
	// free-store에 2개분 공간 할당
	// "4월 21일"을 갖는 YString instance를 Stack에 생성자로 생성
	// 만들어진 instance와 기존 벡터가 가진 원소를
	// 벡터가 관리하는 새로운 free-store에 이동생성
	// push_back 종료, 기존 벡터 소멸 -> instance 2개 소멸
	v.push_back("4월 21일");
	std::cout << std::endl;

	// 동일 과정
	v.push_back("화요일 56교시");

	std::cout << "관찰" << std::endl;
	std::cout << v[ 0 ] << v[ 1 ] << v[ 2 ] << std::endl;
	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 14:52:16.9381461
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;
	std::vector<YString> v;

	// 매 번 벡터 이동/소멸 없이 미리 공간 할당
	v.reserve(10);

	v.push_back("2026년");
	std::cout << std::endl;

	v.push_back("4월 21일");
	std::cout << std::endl;

	// 동일 과정
	v.push_back("화요일 56교시");

	std::cout << "관찰" << std::endl;
	std::cout << v[ 0 ] << v[ 1 ] << v[ 2 ] << std::endl;
	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 14:55:42.6153885
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	std::vector<YString> v;

	// 매 번 벡터 이동/소멸 없이 미리 공간 할당
	v.reserve(10);

	v.push_back("2026년");
	std::cout << std::endl;

	v.push_back("4월 21일");
	std::cout << std::endl;

	// 동일 과정
	v.push_back("화요일 56교시");

	observe = true;
	for ( YString& ys : v ) {
		ys.show();
	}
	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 15:04:01.1034932
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <vector>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	observe = true;
	std::vector<YString> v;

	// 매 번 벡터 이동/소멸 없이 미리 공간 할당
	// 생성자는 메모리를 만들지 않고, 소멸자는 메모리를 소멸시키지 않는다.
	// 생성자는 할당받은 메모리를 채우고, 소멸자는 메모리 해제 전 비운다.
	// Modern C++ 이전 -> vector.reserve(10) 해서 메모리 할당받은 뒤
	// 생성된 임시 객체를 이동

	// Modern C++
	// 메모리 할당과 동시에 임시 객체 생성 없이 바로 채운다.
	// emplace_back() - 인자로 (Ystring{}) 하여 임시 객체 생성 않도록 하자.
	// 재료(생성자 인자)만 준다 생각하자

	v.reserve(10);

	v.emplace_back("2026년");
	std::cout << std::endl;

	v.emplace_back("4월 21일");
	std::cout << std::endl;

	// 동일 과정
	v.emplace_back("화요일 56교시");

	observe = false;

	save("main.cpp");		// "메인.cpp"를 저장하자
}

====================================================================
저장 시간 - 2026-04-21 15:19:19.2230062
====================================================================

//------------------------------------------------------------------
// 2026 1학기 STL				4월 21일					(8주 1일)
//------------------------------------------------------------------
// STL Container - Containers are objects that store other objects.
// array<T, N>	- 유일하게 크기 고정된 자료구조
// vector<T>	- dynamic array
//------------------------------------------------------------------

#include <iostream>
#include <fstream>
#include <array>

#include "save.h"
#include "YString.h"

extern bool observe;

int main( )
{
	save("main.cpp");		// "메인.cpp"를 저장하자

	// [문제] "main.cpp"의 소문자 개수를 세서 다음과 같이 출력하라
	// a - 10
	// b - 3
	// 중간 생략

	std::ifstream in{ "main.cpp" };

	std::array<int , 26> count{};

	if ( not in ) {
		return 20260421;
	}

	char c;
	while ( in >> c ) {
		if ( islower(c) )
			++count[ c - 'a'];
	}

	for ( int i = 0; i < count.size( ); ++i ) {
		std::cout << static_cast< char >('a' + i) << " - " << count[ i ] << std::endl;
	}
}

